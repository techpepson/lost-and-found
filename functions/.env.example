# Copy to functions/.env.<your-project-id> before deployment.
# Empty allows any verified email for an academic demonstration.
# For a campus pilot, confirm actual eligible domains with the operator.
ALLOWED_EMAIL_DOMAINS=
# Enable only after integrating and testing a native App Check provider.
ENFORCE_APP_CHECK=false
